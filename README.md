Inverted Pascal Triangle Pattern in JavaScript

This program prints an inverted Pascal triangle pattern.

The outer loop controls the rows.
The first loop prints spaces to create the inverted shape.
The second loop prints numbers using a formula to generate Pascal triangle values.

Each row prints fewer numbers than the previous row.
